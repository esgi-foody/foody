{% extends 'base.html.twig' %}

{% block title %}Foody | Profile > {{ user.pseudo }}{% endblock %}

{% block body %}
    <h1>{{ user.pseudo }}</h1>


    <table class="table">
        <tbody>
        <tr>
            <th>Pseudo</th>
            <td>{{ user.pseudo }}</td>
        </tr>
        <tr>
            <th>Biography</th>
            <td>{{ user.biography }}</td>
        </tr>
        </tbody>
    </table>
    <a class="btn btn-primary" href="{{ path( follow.path , {'user': user.username}) }}">{{ follow.title }}</a>

    {#<a href="{{ path('app_back_category_index') }}">back to list</a>#}

    {#<a href="{{ path('app_back_category_edit', {'id': category.id}) }}">edit</a>#}

    {#{{ include('back/category/_delete_form.html.twig') }}#}
{% endblock %}
