<div class="navbar-fixed">
    <nav class="navbar">
        <div class="nav-wrapper nav">
            {% if (is_granted('IS_AUTHENTICATED_FULLY')) %}
                <ul id="nav-mobile" class="left hide-on-med-and-down">

                    <li><a href="{{ path('home') }}" class="logo"><img class="logo__img" src="{{ asset('build/images/logo.svg')}}"></a></li>

                    <li><a href="{{ path('home') }}" class="valign-wrapper {{ app.request.get('_route') == 'home' ? 'active' }}">
                            <i data-feather="home" id="home"  class="nav-icon" stroke-width="2" width="24" height="24" ></i>
                            Accueil
                        </a>
                    </li>
                    <li><a href="{{ path('app_front_explorer_index') }}" class="valign-wrapper {{ app.request.get('_route') == 'app_front_explorer_index' ? 'active' }}">
                            <i data-feather="search" id="explorer" class="nav-icon" stroke-width="2" width="24" height="24" ></i>
                            Explorer
                        </a>
                    </li>
                    <li><a href="{{ path('recipe_new') }}" class="valign-wrapper {{ app.request.get('_route') == 'recipe_new' ? 'active' }}">
                            <i data-feather="plus-square" id="add" class="nav-icon" stroke-width="2" width="24" height="24"></i>
                            Ajouter
                        </a>
                    </li>
                </ul>

                <ul class="right hide-on-med-and-down">
                    <li><a href="{{ path('app_front_notifications_index') }}"  class=" {{  notificationsNumber() ? 'valign-wrapper '}}{{ app.request.get('_route') == 'app_notifications_index' ? 'active' }}" style="padding-top:10px;"><i data-feather="bell"></i><span class="notification-badge">{{ notificationsNumber() ?notificationsNumber() }}</span></a></li>
                    <li><a class="dropdown-trigger" href="#!" data-target="dropdown1" style="padding-right: 40px;display: flex;align-items: center;">@{{ app.user.username }}
                            {% if app.user.imageName %}
                                <img src="{{ asset('uploads/images/users/'~app.user.imageName)}}" class="responsive-img circle z-depth-5 " style="max-height:3em;width: 3em; height: 3em;margin-left: 10px;">
                            {% else %}
                                <img src="{{ asset('build/images/defaultUser.png')}}" class="responsive-img circle z-depth-5  " style="max-height:3em;margin-left: 10px;">
                            {% endif %}
                        </a>
                    </li>
                </ul>

                <ul id="dropdown1" class="dropdown-content">
                    <li><a class="red-text" href="{{ path('app_front_profile_show', {'username' : app.user.username}) }}">Profil</a></li>
                    <li><a class="red-text" href="{{ path( 'app_front_favorite_show' , {'username': app.user.username}) }}">Favoris</a></li>
                    <li><a class="red-text" href="#!">Paramètres</a></li>
                    <li class="divider"></li>
                    <li><a class="red-text" href="{{ path('app_front_auth_logout') }}">Déconnexion</a></li>
                </ul>

            {% else %}
                <ul>
                    <li><a href="{{ path('app_front_auth_login') }}">Connexion</a></li>
                    <li><a href="/register">Inscription</a></li>
                </ul>
            {% endif %}

        </div>
    </nav>
</div>