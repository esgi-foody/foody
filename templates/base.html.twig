<!DOCTYPE html>
<html style="position: relative;">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Foody{% endblock %}</title>
        <link rel="shortcut icon" href="{{ asset('build/images/logo.png')}}" />

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {{ include('front/nav.html.twig') }}
        <div class="{% if app.user %} container {% endif %}">
            {% block body %}{% endblock %}
        </div>
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </body>
</html>
